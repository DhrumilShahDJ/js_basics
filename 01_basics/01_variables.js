const userId = 456
let userName = "Dhrumil Shah"
var userPassword = "12345"
userAge = 78

// userId = 500 // Not allowed to be change

// In tradition js the var is not followed the block or scoped behavior.
// That's why the preferd choise for variable declaration is let.

userName = "dshah"
userPassword = "56789"
userAge = 68
console.table([userId, userName, userPassword, userAge])